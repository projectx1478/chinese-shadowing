# SESSION

最終更新：2026-07-23

---

# 現在の状況

PR #9〜#17（CLAUDE.md整備、Playwrightテスト基盤、ライトモード視認性、AI添削箇条書き化、シャドーイングステップ統合、AIコーチアバター、PROJECT.md「8. 練習スタイル追加要件」追加）はいずれもmainにMerge済み。今回はPROJECT.md 8章で定義した4新規練習モードの実装に着手。ユーザーから「デバッグ等は自動で行い、判断が必要なところまで進めて」と指示されたため、優先順位（8-3→8-1→8-2→8-4）に沿って自動実装中。8-3（ミニマルペア聞き分け）実装完了・PR作成予定（下記参照）、8-1〜8-4は次回以降続行。

---

# 今回完了した内容

過去分（PR #9〜#17：CLAUDE.md整備・Playwrightテスト基盤・ライトモード視認性・AI添削箇条書き化・シャドーイングステップ統合・声調ピッチグラフ文字ラベル・ニュースリンク精度改善・AIコーチアバター・PROJECT.md 8章追加）は各PRの説明を参照（すべてMerge済み・詳細は割愛）。

- **8-3 ミニマルペア聞き分けモードを実装**（PR作成予定）
  - `MINIMAL_PAIRS`静的配列（声調/子音/母音、計26ペア）を追加。API不使用
  - 練習モード選択に「👂 ミニマルペア聞き分け」を追加（`setMode('minimal')`）。開始すると`S.practiceMode==="minimal"`となり、`render()`が既存のシャドーイング/ディクテーションフローと分岐して`renderMinimalPairs()`専用画面を表示（ステップバーは非表示）
  - フローはA・B参照音再生→どちらか一方をランダム再生→2択回答→即時正誤表示→1.1秒後に自動で次の問題（10問固定）。「同じ/違う」判定形式は未実装（2択のみ）
  - カテゴリ別正答率を`S.minimalPairStats`としてFirestoreに累積保存し、結果画面で表示
  - 実装中に発見したバグ：`nextStep()`の`orderMap`に"minimal"が無いため、ミニマルペア中に矢印キー等のショートカットを押すと誤って`loadSentence()`（AI呼び出し）に入ってしまう問題があり、ミニマルペア中は全ショートカットを無効化する対応を追加
  - Playwrightテストを3件追加（モード開始・正誤判定とカテゴリ集計・ショートカット誤発火防止）、既存6件と合わせて計9件pass

---

# 現在作業中

- ユーザー指示「デバッグ等は自動で行い、判断が必要なところまで進めて」に基づき、PROJECT.md 8章の4モードを優先順位（8-3→8-1→8-2→8-4）で自動実装中
- 8-3は実装完了、PR作成予定。続けて8-1（穴埋めディクテーション）に着手予定

---

# 次回最優先作業

- **PROJECT.md 8章の残り3モードの実装継続**：8-1（穴埋めディクテーション）→8-2（音声のみ内容理解クイズ）→8-4（バックワードビルドアップ）の順で自動実装を継続する（ユーザーからの継続指示あり）
- 8-3ミニマルペア聞き分けを実機（マイク不要・音声再生のみ）で動作確認。特にWeb Speech APIでの声調・子音母音の聞き分けやすさ（TTS音声の品質に依存するため、期待通り聞き分けられるか）
- 前回セッションからの継続課題（実機確認は本セッションでは未実施）
  - ダークモード視認性・パース不整合・ピッチ始点の3件を実機で改めて動作確認
  - PR #12のライトモード視認性修正・レスポンシブ対応を実機（iPad Pro / AQUOS9・PC）で確認
  - 単語・文法図鑑の正答率が複数回の練習を経て正しく蓄積されるか実運用で確認
  - AIコーチの復習フロー（reviewQueueのSRS更新）を複数日にまたいで運用検証
  - 声調ピッチグラフの精度・文字ラベル対応を実機の複数録音で確認
  - PR #14のシャドーイング③確認・音読統合ステップ、ニュースリンクの検索結果の見え方を実機で確認
- AI解析速度の改善（②発音詳細評価を①の結果に依存させず並列実行する案）を実施するか要検討。採点の一貫性とのトレードオフがあるため、着手前に方針確認が必要
- 追加したPlaywrightスモークテストを、今後の修正PRのたびに実行する運用を定着させる（現状はローカル実行のみ、CI連携は未着手）
- ファイル名は今後小文字統一を徹底する（`Style.css`のような大文字混在は404の原因になるため）

---

# 保留事項・既知の問題

- Gemini音声合成（より自然なTTS）の導入は見送り中（無料枠のRPD制限のため）。将来的に制約が緩和されれば再検討の余地あり
- OSのTTS音声設定画面へアプリから直接遷移させる手段はない（Web標準APIでは非対応、iOS/Android共通）。案内文言での対応は今回見送り
- 声調概形のオーバーレイは音節の時間割り当てが均等割りの近似であり、精度は粗め（仕様として許容済み）
- 状態色（成功/警告/危険）とHSKレベル別の色は、テーマ変数化せず固定のままにしている（どちらのテーマでも視認性が保てるための意図的な設計）
- 発音解説・フィードバック等の一部ボックスは内部で完結した配色（テーマに関わらず固定）としている
- AIコーチの「会話練習」枠は現状シャドーイングモードと同一実装であり、専用の会話継続ロジックは未分離（Phase2で検討）
- Cloudflare Worker（`worker.js`）はGitHubリポジトリに含めていないため、Worker側の変更履歴はPROJECT.mdでのみ管理している点に注意
- `app.js` はさらなる機能別分割（config/firebase/gemini/audio/practice等）を将来的に検討可能だが、巻き上げ依存の読み込み順序リスクがあるため、着手時は依存関係を精査してから段階的に行うこと
- Playwrightスモークテストは認証・録音・実機TTSを対象外としているため、これらの最終確認は引き続き実機（iPad Pro / AQUOS9）で行う必要がある
- AI解析（録音後）の速度改善は、採点の一貫性とのトレードオフがあるため未着手（上記「次回最優先作業」参照）
- ミニマルペア聞き分け（8-3）は「同じ/違う」判定形式は未実装（2択のみ）。`MINIMAL_PAIRS`は26ペアの初期セットのみで、将来的に拡充の余地あり

---

# 注意事項

このファイルをセッションの引き継ぎ情報とする。
新しいセッションでは最初にこのファイルを読む。
チャット履歴には依存しない。

**Claude Codeへの引き継ぎメモ**：本プロジェクトはGitHubリポジトリ（`projectx1478/chinese-shadowing`、mainブランチ）が唯一のソースコード管理場所。Claude Codeで作業する場合も、作業開始時に必ず `PROJECT.md` → `SESSION.md` → `CLAUDE.md` の順で読むこと（PROJECT.md 7章参照）。ファイル構成は `index.html` / `style.css` / `app.js` の3ファイル（2026-07-23分割）。GitHubへの直接push運用ルール（ブランチ→コミット→PR→人がレビュー・Merge）も継続して遵守すること。テスト基盤として `tests/smoke.spec.js`（Playwright）を追加済み（2026-07-23）、修正時は可能な範囲で実行すること。
